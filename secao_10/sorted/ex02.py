numeros = [6, 1, 8, 2]

# Ordena do maior para o menor e transforma em tupla
reverse = tuple(sorted(numeros, reverse=True))
ordenado = set(sorted(numeros))  # Ordena do menor para o maior e transforma em um set
