# Exemplos TypeError
# Ocorre quando uma função/operação ou ação é aplicada a um tipo errado

"""
    # Type int has no len()
    print(len(5))

    # Can only concatenate str (no list) to str
    print('Geek' + [])

    # Index Erros
    # Ocorre quando tentamos acessar um elemento em uma lista ou outro tipo de dado indexado utilizando um indice inválido
    lista = ["Geek"]
    print(lista[0])
    print(lista[0][0])
    """
